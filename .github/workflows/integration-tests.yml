name: Integration Tests

on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:

jobs:
  integration:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Enforce no legacy .save files
        run: |
          found="$(find . -type f -name "*.save" -not -path "./.git/*" -print)"
          if [[ -n "$found" ]]; then
            echo "Found forbidden .save files:"
            echo "$found"
            exit 1
          fi

      - name: Run integration tests
        run: |
          export PYTHONPATH="$GITHUB_WORKSPACE"
          python -c "import sys; print(sys.path[0]); import apps; print('apps import ok')"
          pytest -q tests/integration
